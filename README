## LAB1
### LAB1-SLEEP
1. 首先了解回调和RSICV的特性，在OS中，user和kernel是具有隔离性的，我们现在要写user函数，我们要先看一下kernel给我们封装了什么，等于在写驱动了，就跟着hint，先来到sysproc.c的sys_pause,注释具体写在sys_pause里，再去到usys.S CTRL+F搜索pause，可以看到明确规定了回调是到sys_pause,再到user.h可以看到封装的pause,调用就可以使用了，单位是1/10 s,这是由硬件规定的
2. 调用流程
- pause 在a7执行ecall，然后把pause(n)n的值放到a0，sys_pause回调的时候argint(0,&n),n=a_0寄存器里面的值，这样就知道要延时多久

- 然后再往sys_pause下面走就可以看到，有sleep，sleep里面我也写了注释，获取当前进程的锁 -> 释放调用函数的锁 -> 当前进程的chan（就是中断唤醒的条件）-> 状态机进入sleep模式 -> 切换进程 -> 等待硬件计时到点 -> 在trap.c里面触发中断 
3. 中断触发流程
- trampoline.S19行写了trampoline:trap.c sets stvec to point here,意思只要发生中断/异常这种都会到stvec寄存器，回看trap.c，会看到prepare_return里 w_stvec(trampoline_uservec);将stvec的虚拟地址设置成uservec的地址，这意味着用户态trap的时候，.S先跳到uservec

- 以及p->trapframe->kernel_trap = (uint64)usertrap;，这样kernel方便操作地址，在trampoline.S的97行，#call usertrap() jalr t_0， 在经历一系列汇编操作后地址跳到t_0然后调用usertrap -> devintr() - > clockintr() 
### LAB1-PINGPONG(PIPE)
1. 用到了sys_pipe , write , read
2. 最搞的点其实就是要去看sys_pipe究竟实现了个啥，给它一个数组，会在内核里面给你两个端口，fd[0]是读端口 ， fd[1]是写端口，通过pipe可以实现两个进程之间的通信实现
3. 最后一定要注意user里面比如调write/read,里面的参数通过.S汇编和RISC_V调用约定传入a_0,.._5寄存器，然后内核通过argraw(int n)，n=1-5,去判断读哪个寄存器的值

### LAB1-PRIMES
1. 进程有独立的地址，你的变量的在堆和栈上都是独立的，两个进程之间不能共享变量，必须用到管道
